plugins {
    id "cpp-application"
    id "visual-studio"
    id "xcode"
}

applications {
    targetMachines = [
            machines.windows().x86(), machines.windows().x86_64(),
            machines.macOS().x86_64(),
            machines.linux().x86()
    ]
}