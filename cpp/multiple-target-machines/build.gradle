plugins {
    id "cpp-application"
    id "visual-studio"
    id "xcode"
}

application {
    targetMachines = [
            machines.windows.x86_64,
            machines.macOS.x86_64,
            machines.linux.x86_64
    ]
}
