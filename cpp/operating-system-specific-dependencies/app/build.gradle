plugins {
    id 'cpp-application'
}

application {
    binaries.configureEach {
        dependencies {
            if (targetPlatform.operatingSystem.windows) {
                implementation project(':winConsoleLogger')
            } else {
                implementation project(':ansiLogger')
            }
        }
    }
}
