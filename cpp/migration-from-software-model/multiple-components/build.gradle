apply plugin: 'cpp'
apply plugin: 'org.gradle.samples.software-model-bridge'

model {
    buildTypes {
        debug
        release
    }
    components {
        libraryFoo(NativeLibrarySpec)
        libraryBar(NativeLibrarySpec)
    }
}