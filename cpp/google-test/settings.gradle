// includeBuild('../../../googletest')
import org.gradle.vcs.git.GitVersionControlSpec

sourceControl {
    vcsMappings {
        withModule('org.gradle:googletest') {
            from vcs(GitVersionControlSpec) {
                url = 'https://github.com/gradle/googletest.git'
            }
        }
    }
}
