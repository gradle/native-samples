plugins {
    id 'swift-application'
}

application {
    binaries.configureEach { 
        dependencies { 
            if (targetPlatform.operatingSystem.macOsX) {
                implementation project(':macos-logger')
            } else {
                implementation project(':linux-logger')
            }
        }
    }
}
