plugins {
    id 'cpp-library'
}

library {
    binaries.configureEach(CppSharedLibrary) {
        linkFile = file("/usr/lib/libcurl.dylib")
        runtimeFile = file("/usr/lib/libcurl.dylib")
    }
}
