plugins {
    id 'groovy'
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        "utilities-build" {
            id = "utilities-build"
            implementationClass = "UtilitiesBuildPlugin"
        }
        "list-build" {
            id = "list-build"
            implementationClass = "ListBuildPlugin"
        }
    }
}
