plugins {
    id 'groovy'
    id 'java-gradle-plugin'
}

group = "org.gradle.samples"
version = "1.0"

gradlePlugin {
    plugins {
        library {
            id = "org.gradle.samples.cmake-library"
            implementationClass = "org.gradle.samples.CMakeLibraryPlugin"
        }
        application {
            id = "org.gradle.samples.cmake-application"
            implementationClass = "org.gradle.samples.CMakeApplicationPlugin"
        }
    }
}
