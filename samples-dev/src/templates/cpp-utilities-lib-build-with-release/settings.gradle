rootProject.name = 'utilities'

sourceControl {
    gitRepository("../release-plugin") {
        producesModule("org.gradle.samples:release")
    }
    gitRepository("../list-library") {
        producesModule("org.gradle.cpp-samples:list")
    }
}
